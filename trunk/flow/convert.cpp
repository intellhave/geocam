/*
#include <iostream>
#include <stdio.h>

int main(int argc, char** argv){
  if(argc != 3){
    printf("USAGE: convert A B\n");
    printf("Where A is a Lutz-formatted manifold file\n");
    printf("and B is a name for the output file\n");
    exit(1);
  }

  char* lutzFilename = argv[1];
  char* outputFilename = argv[2];
  make3DTriangulationFile(lutzFilename, outputFilename);
  return 0;
}
*/
